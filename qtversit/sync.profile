# Fake sync.profile
# Used to generate the QtVersit headers
%modules = ( # path to module name map
    "QtVersit" => "$basedir"
);
%moduleheaders = ( # restrict the module headers to those found in relative path
);
%dependencies = (
);
